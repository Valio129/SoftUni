function solve(nums, commands) {
    for (let el of commands) {
        el = el.split(' ')
        switch (commands[0]) {
            case 'contains':
                
                break;
            case 'addMany':
                break;
        
            default:
                break;
        }
    }
}