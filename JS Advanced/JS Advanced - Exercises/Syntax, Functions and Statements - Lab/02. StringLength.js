function solve(a, b, c) {
    const lenghtsSum = a.length + b.length + c.length;
    const avg = Math.floor(lenghtsSum / 3);
    console.log(lenghtsSum);
    console.log(avg);
};
solve('chocolate', 'ice cream', 'cake');